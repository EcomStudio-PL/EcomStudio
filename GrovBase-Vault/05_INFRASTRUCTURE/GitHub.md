# GitHub

Repo: EcomStudio-PL/EcomStudio (legacy name — internal identifier).
Branches: `main` = production source · working branch
`claude/ecomstudio-extract-push-f8ceir` mirrors to main · rollback pointers
under `rollback/*` (tags rejected by remote policy, branches used instead).
History verified secret-clean across all 71 commits (2026-08-28).
