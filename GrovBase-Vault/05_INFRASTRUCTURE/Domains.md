# Domains

Current: ecomstudio-prod.vercel.app (production alias).
TODO (owner): purchase GrovBase domain → add to Vercel project → update
support-email link in app/actions/support.ts → verify sending domain in
Resend and set EMAIL_FROM.
