# Generation Costs

Charge = pricing[resolution] × shots (+ surcharge for engine prompts).
Real provider cost recorded per usage event (actual_api_cost_usd_micros)
→ admin economics pages compute true margin per user/model.
Provider USD prices: TODO VERIFY against current provider price lists —
do not trust stale numbers; update `internal_cost_usd_micros` in admin
after verification. See [[07_FINANCE/API Costs]].
