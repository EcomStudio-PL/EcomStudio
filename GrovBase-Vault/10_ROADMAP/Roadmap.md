# Roadmap
Near: Stripe payments (payments table + webhooks ready-shaped) · legal docs
+ consent checkbox · GrovBase domain + email domain · account deletion flow.
Mid: video generation (UI scaffolded, VIDEO_ENABLED=false until real
backend) · durable rate limiting + queue before ~3k users (see
14_OPERATIONS and scalability notes) · Next 16 migration window.
